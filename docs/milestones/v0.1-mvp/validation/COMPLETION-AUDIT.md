# MVP Goal 完成性审计

> 审计日期：2026-08-23
> 结论：`COMPLETE / GO`
> 原则：只把有当前文件、运行结果或外部验收记录证明的项目标记为完成。

## Goal 1：工程基座

状态：本地完成。

- pnpm workspace、Turborepo、Node 24 版本文件和锁文件已建立，`pnpm install --frozen-lockfile` 通过。
- CLI 使用 Nest Application Context；Nest HTTP Server 的 `/health` 已实际启动并返回 `{"status":"ok"}`。
- SQLite 迁移可重复执行，核心表及发布审计新增列均有自动化测试。
- 核心模型依赖边界和日志明文脱敏有直接测试。
- 当前引擎目录已初始化为 Git 仓库；首次提交为 `9722d73`，当前 `main` 与 `origin/main` 对齐。Codex 未执行 push。
- 从提交基线导出的干净临时目录中，冻结锁文件安装、完整质量门和 CLI 帮助均通过。

## Goal 2：代码解析

状态：本地完成，保留静态分析限制。

- React、Vue 和 Spring Fixture 契约覆盖页面、路由、按钮、字段、请求、跳转、提示、权限、Controller、DTO、校验、Service、状态变化、异常和数据访问。
- `analysis.include` 支持根目录及多模块 `src`；空前端或后端不会记录成功运行。
- 页面跳转不再误识别为 HTTP API；业务快照拒绝悬空证据 ID。
- 连续两次分析生成独立运行和快照，已有自动化验证。
- 真实 CRM 权威运行：`run:d9b67553-4b90-4699-9be6-b320c0049b2b`，快照 `snapshot:4ebbeb2fbfaa4d96`；15,988 条事实、179 个候选、13/255 个前后端未关联项、0 冲突。
- Spring 方法内事实现在绑定具体方法，FeatureLinker 仅沿当前端点、被调用 Service 方法和一层可达数据访问方法扩展，避免整个 Service 类过度关联。三个试点的 155 条后端引用全部通过文件、Commit 和行号复核。
- 分析事实在 Analyzer、应用快照、持久化和 AI Prompt 四个边界执行统一脱敏；端到端测试证明假密钥不会进入内存快照、JSON 或 SQLite。
- 一次性安全迁移已清理现有数据库中的 103,144 条历史事实及关联快照；最新逐记录结构化复核覆盖代码事实、业务快照、文档修订、发布结果和 20 份 JSON 报告，未发现未脱敏凭证或损坏 JSON。迁移幂等标记后重复启动耗时约 1 ms。
- 真实项目仍有 41 个 Tree-sitter 降级文件和 255 个未关联后端端点；这些项目未被隐藏或提升为 verified。

## Goal 3：文档生成、截图和审核

状态：完成。

- 结构化 `DocumentationModel`、证据校验、来源快照/Commit、Markdown、截图导入、本地预览、不可变修订和审核状态已实现。
- 本地预览显示证据 ID 和 blocking review item；批准旧修订不会使后续新修订自动获批。
- 截图命令支持按 Feature ID 关联稳定 Document ID；零参数预览可定位项目最新修订，显式 Markdown 路径仍受支持。
- Composer 校验拒绝将 `AI_INFERENCE` 证据升级为 verified 内容。
- 已生成 5 个真实 CRM 候选；最终选择的三篇均完成真实 AI、整篇人工内容确认和截图整合。线索转化修订 12、项目发起评审修订 10、角色维护修订 9 均已批准。
- Anthropic Messages 兼容接口已真实生成线索转换修订 9；Prompt 移除绝对仓库路径，AI 修订强制阻塞审核。
- 真实诊断确认普通文本、直接 JSON 和 Anthropic `tool_use` 均可用。复杂 Schema 输出会波动；Composer 已加入最多 3 次有限重试、有限机械结构修复和补充 Prompt 证据白名单，非法证据、身份或业务类型仍被拒绝。
- 三个最终样例均至少关联一张真实人工截图；线索转化修订 12 含“关联已有客户”和“新建客户”两张脱敏截图，角色维护保留脱敏页面截图，项目发起评审保留 CRM 发起入口截图。
- 三个最终样例的评审记录结构已补齐；用户追加确认 18 条逐条研发/业务判定均为正确，三篇事实准确率和步骤覆盖率均为 100%。评审起止时间未提供，人工修改耗时无法测量。

## Goal 4：飞书发布

状态：已完成。

- Publisher 接口、内存适配器、飞书 Block 转换、图片上传、有限重试、错误分类、审核门禁和发布审计已实现。
- CLI 真实发布路径使用飞书官方 Node SDK；发布写入前验证知识空间及可选父节点权限。
- 字段说明使用 Table → TableCell → Text 的嵌套 Block。真实最小探针定位到 `1770001` 的根因是 TableCell 缺少必需的 `table_cell: {}` 属性；修复后原生表格、表头和列宽均已真实写入并视觉验证。
- 节点创建后正文失败会保存部分远端映射，重试复用节点，避免重复创建。
- 发布前同时检查 approved 状态和未解决 blocking review item；审核解决记录独立持久化并保持文档修订不可变；截图资源缺失会在远端正文修改前失败。
- 本地三次发布序列覆盖同一修订重复发布及新已批准修订更新，三条 completed 记录仅对应一个远端节点和文档映射。
- CLI 发布前展示空间、父节点、标题、修订和图片数量。
- 真实 Wiki/Docx 权限和测试空间访问已通过；修订 8 首次发布成功，复用部分失败留下的节点并写入 22 个 Block，本地审计为 completed。
- SDK 默认错误日志已静默，应用仅输出裁剪后的错误类别和业务错误，不再打印含 Authorization Header 的 Axios 请求对象。
- SDK 图片上传异常也使用裁剪后的响应业务码和消息；自动化测试证明原始异常中的伪造 Authorization 内容不会进入最终错误。
- 当前审计累计为 7 次 completed、9 次 failed（包含权限诊断和协议试验）。权限生效后修订 12 最近 6 次均稳定重复发布，均复用原节点和文档 Token；2026-08-23 最新只读复核为 29 个顶层 Block、2 个图片 Block、1 个原生表格、0 个正文一级标题。更新流程已改为完整追加新正文并绑定图片后才删除旧正文，失败时保留旧内容。
- 发布错误持久化前执行敏感文本脱敏；飞书 SDK 继续使用静默 logger，避免请求头进入终端日志。

## Goal 5：真实项目验收

状态：完成。

- 已固定指标算法；用户已确认线索转化、项目发起评审和角色维护 3 个最终样例，并指定本人同时担任研发与业务评审人。
- 四个候选已分别建立独立分析运行、业务快照和待审核修订；两个入选的新增样例已关联脱敏截图并通过 localhost 预览验证。
- 独立临时数据目录已完整重复本地 CLI `analyze → generate → screenshot add → preview`，预览返回 200，截图后新修订保持 `needs_review` 和 blocking 审核项，临时目录已删除。外部飞书步骤尚未包含在这次干净复跑中。
- 已验证静态分析、业务聚合、结构化文档、Markdown、审核门禁及本地发布契约。
- 已通过登录态飞书页面复核目录、正文排版和两张图片位置，并据此去除了页面原生标题下方的重复正文标题。
- 用户已确认三个样例逐条研发/业务人工判定均为正确，并确认最终飞书页面排版、表格和图片位置通过验收。真实 AI 可通过进程环境调用，不持久化凭证到项目。
- MVP 剩余验收已完成，最终结论为 `GO`。

## 当前残余风险

1. Anthropic Messages 兼容服务对复杂 Schema 的遵循仍有波动，现有有限重试已得到一个合法修订；企业源码摘录的合规范围仍未形成书面结论。
2. 41 个大型或复杂 Java 文件仍需要评估 Tree-sitter 升级、分段策略或 JavaParser Sidecar。
3. 用户未提供评审起止时间，人工修改耗时无法测量。
